#' get_metadata_datasets
#'
#' Gather all the metadata corresponding to every nid for datasets
#'
#' @param nids_datasets list: list of nodes to gather data for
#' @param credentials list: object returned by the get_credentials() function
#'
#' @return list
#' @export
#'


# might change workflow, haven't decided yet
get_metadata_datasets <- function (nids_datasets,
                                   credentials = list(cookie = dkanr::get_cookie(),
                                                      token = dkanr::get_token())){
  metadata_datasets <- get_metadata_all(nids_datasets, credentials)
  return(metadata_datasets)
}
